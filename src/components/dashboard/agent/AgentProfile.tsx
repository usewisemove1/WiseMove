"use client";
import { useState } from "react";
import { BadgeCheck, Pencil } from "lucide-react";
import { Button } from "@/components/ui/button";

const fields = [["Agency", "Brightstone Realty"], ["CAC RC number", "RC 1845027"], ["Licence", "ESVARBON-AG-4421"], ["Location", "Lekki, Lagos"], ["Email", "agent@amaafi.com"], ["Phone", "+234 801 234 5678"]];
export default function AgentProfile() { const [editing, setEditing] = useState(false); return <div className="max-w-3xl space-y-6"><div className="flex items-start justify-between"><div><h1 className="text-2xl font-bold sm:text-3xl">Agent profile</h1><p className="mt-1 text-sm text-muted-foreground">Your public professional details.</p></div><Button variant="outline" onClick={() => setEditing((v) => !v)}><Pencil className="h-4 w-4" /> {editing ? "Cancel" : "Edit profile"}</Button></div><div className="rounded-2xl border border-border bg-white p-5 shadow-sm sm:p-7"><div className="flex items-center gap-3 border-b border-border pb-5"><span className="flex h-12 w-12 items-center justify-center rounded-full bg-primary text-lg font-bold text-white">AO</span><div><p className="font-bold">Agent account</p><span className="inline-flex items-center gap-1 text-xs font-semibold text-emerald-700"><BadgeCheck className="h-3.5 w-3.5" /> Verified professional</span></div></div><div className="mt-5 grid gap-4 sm:grid-cols-2">{fields.map(([label,value]) => <label key={label} className="text-sm font-semibold text-foreground">{label}{editing ? <input defaultValue={value} className="mt-1.5 w-full rounded-lg border border-input px-3 py-2 text-sm font-normal" /> : <p className="mt-1 text-sm font-normal text-muted-foreground">{value}</p>}</label>)}</div><label className="mt-5 block text-sm font-semibold">Professional bio{editing ? <textarea rows={4} defaultValue="Helping clients find trusted homes across Lagos." className="mt-1.5 w-full rounded-lg border border-input px-3 py-2 text-sm font-normal" /> : <p className="mt-1 text-sm font-normal leading-6 text-muted-foreground">Helping clients find trusted homes across Lagos, with transparent advice from viewing to closing.</p>}</label>{editing && <Button className="mt-6" onClick={() => setEditing(false)}>Save changes</Button>}</div></div>; }
